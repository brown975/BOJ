#include <stdio.h>
int a, s, d, f, q, w, e, r;
int main() {
	scanf("%d", &a);
	for (int g = 1; g <= a; g++) {
		scanf("%d%d", &s, &d);
		(!s || !d ? f : s < 0 ? d < 0 ? e : w : d < 0 ? r : q)++;
	}
	printf("Q1: %d\nQ2: %d\nQ3: %d\nQ4: %d\nAXIS: %d", q, w, e, r, f);
}
